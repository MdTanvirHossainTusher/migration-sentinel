{
  "title": "CREATE INDEX without CONCURRENTLY on a large table",
  "description": "orders has ~8M rows. A plain CREATE INDEX holds a SHARE lock for the whole build, blocking every write.",
  "hard": false
}
